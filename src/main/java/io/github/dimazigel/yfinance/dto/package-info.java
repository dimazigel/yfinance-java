/** Raw records mirroring Yahoo's JSON. Every field may be absent, so every component is {@code @Nullable}. */
@NullMarked
package io.github.dimazigel.yfinance.dto;

import org.jspecify.annotations.NullMarked;
